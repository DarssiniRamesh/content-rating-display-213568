pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "vod_content_rating_frontend"

include(":app")
include(":list")
include(":utilities")
