Nested Gradle Wrapper

This directory is the Android Gradle root and includes wrapper metadata:
- gradle/wrapper/gradle-wrapper.properties

Use:
- ./gradlew build

If your environment requires the wrapper JAR under this directory, generate it with:
- ./gradlew wrapper
