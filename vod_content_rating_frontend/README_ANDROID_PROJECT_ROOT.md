Android Gradle Project Root

The Android/Kotlin project root is located at:
content-rating-display-213568/vod_content_rating_frontend

Use this directory as the working directory for builds and analysis.

Commands (run from the above directory):
- Build: ./gradlew :app:assembleDebug
- Test: ./gradlew test

Project files to detect:
- settings.gradle.dcl
- build.gradle (root)
- app/, list/, utilities/ modules
