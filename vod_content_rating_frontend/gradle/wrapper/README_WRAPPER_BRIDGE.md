This repository uses a Gradle wrapper bridge.

Primary Android workspace:
- content-rating-display-213568/vod_content_rating_frontend

To build the Android app from repo root:
- ./gradlew :app:assembleDebug

The wrapper scripts at repo root delegate to the Android workspace wrapper.
