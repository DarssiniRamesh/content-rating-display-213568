This folder contains the Gradle Wrapper metadata for the nested Android project.
The wrapper JAR is available at:
- gradle/wrapper/gradle-wrapper.jar

If your tool launches from this directory, it should execute:
- ./gradlew :app:assembleDebug
