# Gradle Wrapper (Android Project Root)

Use the wrapper here to build the Android app:
  ./gradlew :app:assembleDebug

Modules:
- :app
- :list
- :utilities

If wrapper JAR is missing, it will be downloaded automatically on first run.
