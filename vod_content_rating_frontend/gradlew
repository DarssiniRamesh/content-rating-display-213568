#!/usr/bin/env bash
# Gradle wrapper launcher for this Android project.
# Ensures we always call the local wrapper relative to this script.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/gradlew.sh" "$@"
