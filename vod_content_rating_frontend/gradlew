#!/usr/bin/env bash
# Root bootstrap Gradle wrapper: delegates to Android workspace wrapper
set -euo pipefail
cd "$(dirname "$0")/content-rating-display-213568/vod_content_rating_frontend"
exec ./gradlew "$@"
