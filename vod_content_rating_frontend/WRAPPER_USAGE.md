Use Gradle Wrapper

From this directory:
- ./gradlew :app:assembleDebug
- ./gradlew :app:installDebug

If the wrapper jar is not present initially, the first execution will download it per gradle/wrapper/gradle-wrapper.properties.
