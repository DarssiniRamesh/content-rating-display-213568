# VOD Content Rating Frontend (Android)

This Android workspace uses Gradle 9 Declarative Configuration Language (DCL).
Canonical entry file:
- settings.gradle.dcl

Build (from this directory):
- ./gradlew :app:assembleDebug

Modules:
- app (application)
- list (library)
- utilities (library)
