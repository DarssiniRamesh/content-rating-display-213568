# Workspace Runner

This folder contains the Android project in vod_content_rating_frontend/.

Common commands:
- ./gradlew :app:assembleDebug  (bridged to subproject wrapper)
- Or cd vod_content_rating_frontend && ./gradlew :app:assembleDebug
