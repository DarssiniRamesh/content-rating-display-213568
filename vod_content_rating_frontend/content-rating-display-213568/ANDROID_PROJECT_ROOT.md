# Android Project Root

The Android Gradle project is nested here:
- Root: `vod_content_rating_frontend/`
- Primary app module: `vod_content_rating_frontend/app/`

Use Gradle wrapper under `vod_content_rating_frontend/gradlew`:
- Build: `./gradlew :app:assembleDebug`
- Test: `./gradlew test`
