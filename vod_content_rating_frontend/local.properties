# This file is for analyzer discovery only; Android Studio/CI should manage the SDK path separately.
# sdk.dir will be set by the Android build environment. Do not hardcode paths here in CI.
# sdk.dir=/path/to/android/sdk
