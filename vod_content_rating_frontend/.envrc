# direnv/auto-shell helper. Allows shells that support direnv to auto-cd for Gradle operations.
export ANDROID_GRADLE_ROOT="content-rating-display-213568/vod_content_rating_frontend"
echo "Tip: cd $ANDROID_GRADLE_ROOT && ./gradlew build"
