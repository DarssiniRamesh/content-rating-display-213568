# Preview Launcher

The class org.example.app.ui.PreviewLauncherActivity opens the rating preview directly with seeded data.
Use it as an alternative MAIN/LAUNCHER in analyzer contexts that cannot follow deep navigation flows.
