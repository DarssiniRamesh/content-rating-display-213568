Gradle Wrapper (Nested Project)

This directory is the Android Gradle project root.

Use:
- ./gradlew :app:assembleDebug

If your tool requires a wrapper metadata folder, gradle/wrapper/gradle-wrapper.properties is provided here.
