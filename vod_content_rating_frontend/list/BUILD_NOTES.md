# Build Notes - :list module

This is an Android library module built as part of the Declarative Gradle project.

How to build (from Android project root):
- ./gradlew :list:assemble

Notes:
- The module is included in settings.gradle.dcl (`include("list")`).
- It is used by the `:utilities` module.
