/*
Bridge build.gradle for :app (actual config is in build.gradle.dcl).
Exists for analyzers that expect a traditional Groovy build file at the module root.
*/
plugins {
    // no-op: real configuration uses Declarative Gradle
}
tasks.register("assembleDebugBridge") {
    group = "build"
    doLast {
        println("Run from workspace root: ./gradlew :app:assembleDebug")
    }
}
