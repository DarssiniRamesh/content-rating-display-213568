# App Module Discovery

Launcher Activity:
- org.example.app.ui.IndiceActivity

Deep Link:
- app://rating/show (handled by org.example.app.ui.DeepLinkLauncherActivity)

Build (from Android workspace root):
- ./gradlew :app:assembleDebug
