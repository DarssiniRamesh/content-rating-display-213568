package org.example.app.data

// PUBLIC_INTERFACE
object Defaults {
    /** Default max display time for the rating screen (seconds). */
    const val MAX_DISPLAY_TIME_SEC: Int = 60
    /** Default rolling credits start time (seconds). */
    const val DEFAULT_ROLLING_CREDITS_SEC: Int = 3
    /** Default rating panel display time (seconds). */
    const val DEFAULT_DISPLAY_TIME_SEC: Int = 10
}
