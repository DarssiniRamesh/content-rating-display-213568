package org.example.app.ui

import org.example.app.R

/**
 * PUBLIC_INTERFACE
 * ResourceProbe provides a simple method to reference the R class
 * to help some analysis pipelines validate resource compilation.
 */
object ResourceProbe {
    // PUBLIC_INTERFACE
    fun idOfIndiceTitle(): Int = R.id.indiceTitle
}
