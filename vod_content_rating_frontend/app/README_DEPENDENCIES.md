# Dependencies

This module uses Gradle 9 Declarative (.dcl). Dependencies are declared under the `dependencies {}` block.

Added for menu/UI compatibility:
- androidx.appcompat:appcompat:1.7.0

Build:
- ./gradlew :app:assembleDebug
