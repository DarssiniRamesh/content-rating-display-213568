# App Module Tasks (Bridge)

Common Gradle tasks:
- :app:assembleDebug
- :app:assembleRelease

Note: Configuration is via Declarative Gradle (.dcl). Use:
  ./gradlew :app:assembleDebug
from the Android project root.
